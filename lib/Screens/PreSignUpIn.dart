import 'package:flutter/material.dart';

class PreSignUpIn extends StatelessWidget {
  const PreSignUpIn({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
        // body: ,
        );
  }
}
